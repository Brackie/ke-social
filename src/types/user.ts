export interface User {
    id: number
    name: string
    email: string
    location?: string
    bio?: string
    profile_image_url?: string
    cover_image_url?: string
}

export interface Alert {
    
}